<?xml version="1.0"?>
<!-- Note: the eXeLearning static editor lives under dist/static/ but is NOT
     committed to the repository (see .gitignore); it is fetched at build/release
     time or installed from GitHub by an admin. It is therefore NOT declared here,
     because Moodle's `grunt ignorefiles` stats every <location> and aborts on a
     missing path, which breaks `moodle-plugin-ci install`. When dist/static/ is
     bundled into a release zip, scripts/package.sh stamps a dist/static
     declaration into the ZIP's copy of this file automatically. -->
<libraries>
  <library>
    <location>assets/scorm/SCORM_API_wrapper.js</location>
    <name>pipwerks SCORM API Wrapper</name>
    <description>Unmodified upstream pipwerks SCORM wrapper used to bridge gradable iDevices with the LMS SCORM runtime (pipwerks/scorm-api-wrapper@82e455b, byte-identical).</description>
    <version>1.1.20180906</version>
    <license>MIT</license>
  </library>
  <library>
    <location>assets/scorm/SCOFunctions.js</location>
    <name>eXeLearning SCORM 1.2 runtime</name>
    <description>eXeLearning project code, not a third-party library: the SCORM 1.2 runtime the editor assembles into every package it exports (the public/app/common/scorm/scorm12/ layers, built by buildScorm12RuntimeFiles() in https://github.com/exelearning/exelearning), vendored unmodified and installed into every package this plugin extracts. Declared here, like the editor under dist/static in the release ZIP, because it is not under this plugin's licence; the commit it was built from and the digests of both runtime files are pinned in assets/scorm/SOURCE. The version is the runtime stamp in the file header.</description>
    <!-- Development runtime from eXeLearning main at 37922ad8586bb38974a6c32208ef940a1ea680fb; see SOURCE. -->
    <version>v0.0.0-alpha</version>
    <license>AGPL-3.0-or-later</license>
  </library>
</libraries>
